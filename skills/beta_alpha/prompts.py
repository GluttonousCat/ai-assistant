"""
beta_alpha 模块提示词 (beta 链综述/选链/研报环节抽取 + alpha 四象限综述)
专业知识放 prompt 不放代码; JSON 花括号一律 {{}} 转义
"""

# ---------- beta: 用户话术 -> 链/环节解析 (规则子串匹配失败时兜底) ----------
BETA_CHAIN_PICK_PROMPT = """你是产业链分析助手。根据用户的话术判断 TA 想分析哪条产业链、哪个环节。

可选产业链 (chain_id | 名称 | 环节列表):
{chains}

规则:
- 用户描述一个趋势或需求 (如"AI服务器放量") 时, 选择最相关的产业链
- 用户提到具体环节词 (如"光模块"、"覆铜板") 时, 一并给出 node_id, 否则 node_id 为 null
- 找不到合适产业链时 chain_id 给 null

用户输入: {text}

严格只输出 JSON:
{{"chain_id": "ai_compute 或 null", "node_id": "环节id 或 null", "reason": "一句话理由"}}"""


# ---------- beta: 链综述 (全链模式) ----------
BETA_SUMMARY_PROMPT = """你是产业链投研分析师。基于以下结构化数据写一段产业链 Beta 分析, 面向个人投资者。

产业链: {chain_name} — {chain_desc}
驱动因素: {drivers}
行情基准: {benchmark} (环节组合为流通市值加权, 超额收益=环节组合/基准-1)
数据窗口: 近120交易日

环节量化数据 (JSON):
{node_table}

各环节标的与证据 (JSON, tier: 强/中/弱; mainbz_share: 主营构成中该环节业务收入占比%):
{members}

写作要求:
1. 全部使用简体中文
2. 结构 (markdown):
   ## 链条总览 — 2-3 句: 链条逻辑与驱动
   ## 环节行情结构 — 按超额收益排序点评各环节 (120日与20日结合, 指出谁领涨谁滞后、上游还是下游先动)
   ## 关键环节与标的 — 挑 4-6 个代表环节, 每个环节列出强档标的及其证据 (主营占比/研报热度), 用表格: | 标的 | 环节 | 主营占比 | 证据 |
   ## 小结 — 一段话: 当前链条的资金焦点、相对滞后的环节、需要跟踪的信号
3. 所有数字必须来自上方数据, 禁止编造或推算; 数据缺失就写"数据不足"
4. 不写投资建议, 保持客观描述"""


# ---------- beta: 单环节下钻模式 ----------
BETA_NODE_PROMPT = """你是产业链投研分析师。用户想深入了解「{chain_name}」中的「{node_name}」环节。

该环节关键词: {keywords}
环节量化数据: {metrics}
环节内标的与证据 (JSON, tier 强/中/弱; mainbz_share=主营占比%; report_hits=近1年研报数):
{members}
研报环节热度: 近半年 {heat} 篇研报提及

写作要求:
1. 全部使用简体中文
2. 结构 (markdown):
   ## {node_name} 环节概览 — 该环节在链条中的位置与作用 (结合关键词)
   ## 行情与景气 — 超额收益/营收增速解读, 缺数据则说明
   ## 核心标的 — 用表格: | 标的 | 档位 | 主营占比 | 主营业务 | 研报数 |; 表格后各用一句话点评强档标的的逻辑
3. 数字必须来自数据, 禁止编造; 不写投资建议"""


# ---------- beta: 研报 -> 产业链环节抽取 (每日链路动态增量) ----------
CHAIN_EXTRACT_PROMPT = """你是产业链知识抽取器。从研报正文中抽取涉及的产业链环节/产品/材料及其关联公司。

抽取规则:
- segment: 环节或产品/材料的中文短语 (如: 光模块、HBM、覆铜板、电子布、液冷、先进封装), 保留原文表述, 不要自创
- direction: 环节位置: upstream(上游材料/设备) / midstream(中游制造) / downstream(下游应用) / other(不确定)
- companies: 文中与该环节强相关的公司名 (原文表述, 最多4个, 无则给空数组)
- confidence: 0-1, 报告是否实质性讨论该环节 (仅顺带提及给低分)
- 只抽取制造业/科技产业环节, 宏观/策略/利率/纯消费类内容返回空列表; 最多 12 个环节

研报正文:
{report_text}

严格只输出 JSON:
{{"segments": [{{"segment": "光模块", "direction": "midstream", "companies": ["某公司"], "confidence": 0.9}}]}}"""


# ---------- forge: 种子链生成 (LLM 草稿 + 命中率反馈修正) ----------
CHAIN_FORGE_PROMPT = """你是 A 股产业链架构师。根据主题设计一条产业链种子模板 (YAML)。

下面是上市公司主营构成披露原文的随机抽样 (真实语料, keywords 要贴近这类措辞):
{corpus_sample}

输出格式 (YAML, 字段严格如下, 不要输出任何其他文字):
chain_id: 小写英文下划线
name: 中文链名
desc: 一句话链条逻辑
drivers: [2-4个驱动因素]
nodes:                       # 6-12 个
  - {{id: 小写英文下划线, name: 中文环节名, keywords: [3-8个], sw_l2: [0-3个]}}
edges:                      # [上游id, 下游id], 只引用已定义节点
  - [node_a, node_b]

keywords 设计纪律 (最重要):
- 优先产品口径词, 必须补应用/业务口径词 (公司披露五花八门, 参考上方语料)
- 禁止裸泛词: 整机/树脂/电子/设备/材料/制品/零部件 等, 必须带限定 (如 电子级树脂、半导体设备)
- 英文缩写直接写 (PCB/HBM/CPO/MLCC), 匹配不分大小写
- sw_l2 用申万二级基础词 (元件/半导体/化学制品/专用设备/消费电子/电池/汽车零部件/IT服务等)

主题: {theme}"""

CHAIN_REFINE_PROMPT = """你设计的产业链种子模板经过「关键词 vs 74.5万行主营构成披露」的命中率校验, 结果如下:

{hit_report}

问题定义:
- 命中 0 只的 keyword: 措辞与真实披露不符, 需替换为更贴近产业通用的说法或删除
- 合计 0 只的环节: 整环节关键词全部落空, 用更宽的产业词重写 (但不得违反泛词纪律)

当前 YAML:
{chain_yaml}

按原格式输出修订后的完整 YAML (保持 chain_id 不变, 不要输出任何其他文字)。"""


# ---------- alpha: 四象限综述 ----------
ALPHA_SUMMARY_PROMPT = """你是个股投研分析师。基于以下「预期差四象限」结构化数据, 写一段对 {stock_name}({ts_code}) 的预期差分析。

1) 券商盈利预测分歧 (fin.report_forecast, 已按单位归一到元):
{forecast}

2) 财务动量 (fina_indicator, 报告期同比/单季):
{momentum}

3) 估值水位 (daily_basic 近3年分位, 0=最低 100=最高):
{valuation}

4) 近期研报观点 (知识星球研报库):
{views}

写作要求:
1. 全部使用简体中文
2. 结构 (markdown):
   ## 预测分歧 — 有几家券商覆盖、对什么指标分歧多大 (CV 越大分歧越大)、近月预测在上修还是下修
   ## 基本面验证 — 营收/利润增速与毛利率的边际方向, 是否验证了乐观预期
   ## 估值水位 — PE/PB 分位描述 (低分位=市场预期保守, 高分位=预期已计入)
   ## 预期差解读 — 综合: 当前市场分歧点在哪、基本面走到哪一步、估值给了多少空间; 若数据缺失如实说明
3. 数字必须来自数据, 禁止编造; 无覆盖/无数据就明说
4. 客观陈述, 不写投资建议"""
